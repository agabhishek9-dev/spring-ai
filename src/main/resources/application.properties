spring.application.name=springai
server.port=8081

# Reads API key from environment variable.
spring.ai.openai.api-key=${OPENAI_API_KEY}

# Global default temperature if not overridden elsewhere. These can be used to set configurations globally but for specific changes
# use Prompt object to configure model parameters
spring.ai.openai.chat.options.temperature=1.0

# Optional experimental properties kept for future testing.
# spring.ai.chat.client.enabled=false
# spring.ai.openai.chat.options.model=gpt-4.1
#
logging.level.org.springframework.ai.chat.client.advisor=debug